## API (apps/api)
DATABASE_URL="postgresql://user:password@localhost:5432/cube_draft?schema=public"
PORT=4000
CORS_ORIGIN="http://localhost:5173"

## AI (Anthropic Claude API)
ANTHROPIC_API_KEY="sk-ant-..."

## Web (apps/web) — Vite inlines these at build time
# Local dev (fallback is http://localhost:4000 if not set)
VITE_API_URL="http://localhost:4000"
VITE_WS_URL="ws://localhost:4000"
# Production — set these in Vercel dashboard:
# VITE_API_URL="https://api-production-3badc.up.railway.app"
# VITE_WS_URL="wss://api-production-3badc.up.railway.app"
